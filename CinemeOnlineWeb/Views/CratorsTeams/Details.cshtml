@model CinemeOnlineWeb.CratorsTeam

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.DirectorName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.DirectorName)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.QuantityPeople)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.QuantityPeople)
        </dd>
        <dt class = "col-sm-2">
            Quantity films by CreatorTeam
        </dt>
        <dd class = "col-sm-10">
            @ViewBag.quantityFilms
             <a asp-controller="Films" asp-action = "Index" asp-route-creationTeamId="@Model.CreationTeamId">View</a>
        </dd>
    </dl>
</div>
<div>
    @if(User.IsInRole("admin") || User.IsInRole("editor"))
    {
        <a asp-action="Edit" asp-route-id="@Model?.CreationTeamId">Edit |</a>
    }
    <a asp-action="Index">Look all creators</a>
</div>

@model CinemeOnlineWeb.CratorsTeam

@{
    ViewData["Title"] = "Delete";
}
 
<h1>Delete</h1>

<div id="failSearch" class="alert alert-dismissible alert-danger" style="@(ViewBag.filmsCount==0? "display:none" : "display:block")">
    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    <strong>FAIL! </strong>First delete all films from this team and try again
</div>
<div style="@(ViewBag.filmsCount==0? "display:block" : "display:none")">
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>CratorsTeam</h4>
        <hr />
        <dl class="row">
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.DirectorName)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.DirectorName)
            </dd>
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.QuantityPeople)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.QuantityPeople)
            </dd>
        </dl>
        <form asp-action="Delete">
            <input type="hidden" name="id" value="@Model.CreationTeamId" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
        </form>
    </div>
</div>
            <a asp-action="Index">Back to List</a>
